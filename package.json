{
  "name": "trained-assist-tg-bot",
  "version": "0.1.0",
  "description": "Trained Assist — Telegram bot (Cloudflare Worker)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js src/handlers/message.js src/handlers/commands.js src/handlers/callbacks.js src/handlers/user-mgmt.js src/lib/telegram.js src/lib/kv.js src/lib/agent-client.js && node scripts/check-commands-registry.mjs",
    "set-commands": "node scripts/set-commands.js",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "wrangler": "^3.0.0"
  }
}
